project_name: Sfogliatella
project_short_name: Sfogliatella
protocol_version: "1.0.0"
library_version: "0.1.0"

current_goal: >
  Build a unified time-series ML library (Sfogliatella) supporting regression/classification
  with Transformer, LSTM, RNN, MLP, CNN, XGBoost, TimesFM. JAX-based, CLI-driven, Docker-friendly.

active_tasks: []

completed_tasks:
  - TASK-20260301-001-initial-implementation
  - TASK-20260302-002-gap-fill-timesfm-custom-smoke
  - TASK-20260301-003-fx-subset-all-models-validation
  - TASK-20260301-004-gpu-container-fix-and-kaggle-full-validation

smoke_status:
  date: "2026-03-02"
  command: >
    docker run --rm -e JAX_PLATFORMS=cpu
    -v .:/workspace sfogliatella:dev bash smoke_test.sh
  result: PASSED
  steps_verified:
    - "MLP train (3 epochs)"
    - "MLP resume training (→ 5 epochs)"
    - "MLP batch predict"
    - "eval metrics (MSE/RMSE/MAE/R2/SMAPE)"
    - "HPO all 5 models (Solved)"
    - "HPO with scaling law + JSON output"
    - "Pack model to .sfog archive"
    - "Unpack .sfog archive"
    - "XGBoost train + predict"
    - "LSTM train"
    - "Transformer train"
    - "CNN train"
    - "RNN train"
    - "MLP classification train (binary BCE, fixed shape bug)"
    - "MLP classification predict"
